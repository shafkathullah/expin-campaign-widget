export default function App() {
  return (
    <main className="min-h-screen p-8">
      <header className="mb-8">
        <h1 className="text-2xl font-semibold">Campaign Performance</h1>
        <p className="text-sm text-muted-foreground">
          Build the widget here. Mock API at <code>http://localhost:4000</code> (or use the{' '}
          <code>/api</code> proxy).
        </p>
      </header>

      <section className="rounded-lg border border-dashed p-12 text-center text-muted-foreground">
        <p>This is your starting point. Replace this with the widget.</p>
      </section>
    </main>
  );
}
